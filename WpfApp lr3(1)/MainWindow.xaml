<Window x:Class="ArrayManipulation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Array Manipulation" Height="350" Width="500">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Введіть кількість елементів масиву:"/>
        <TextBox Grid.Row="1" Name="ElementsTextBox"/>

        <Button Grid.Row="2" Content="Обробити масив" Click="ProcessArray_Click"/>

        <TextBlock Grid.Row="3" Text="Результати:" Margin="0,10,0,0"/>
        <TextBox Grid.Row="4" Name="ResultsTextBox" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Visible"/>
    </Grid>
</Window>
