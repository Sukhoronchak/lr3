<Window x:Class="ArrayManipulation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Array Manipulation" Height="350" Width="500">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Кількість рядків масиву:"/>
        <TextBox Grid.Row="1" Name="RowsTextBox"/>

        <TextBlock Grid.Row="2" Text="Кількість стовпців масиву:"/>
        <TextBox Grid.Row="3" Name="ColumnsTextBox"/>

        <Button Grid.Row="4" Content="Обробити масив" Click="ProcessArray_Click"/>

        <TextBlock Grid.Row="5" Text="Результати:" Margin="0,10,0,0"/>
        <TextBox Grid.Row="6" Name="ResultsTextBox" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Visible"/>
    </Grid>
</Window>
